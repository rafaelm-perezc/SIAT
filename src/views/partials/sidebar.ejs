<aside class="w-72 bg-terminal-darkgray border-r border-gray-800 flex flex-col h-screen shadow-2xl">
    <div class="h-20 px-6 flex items-center border-b border-gray-800">
        <div>
            <p class="text-terminal-yellow font-bold text-lg tracking-wide">SIAT</p>
            <p class="text-xs text-gray-400">Sistema Integrado</p>
        </div>
    </div>

    <nav class="flex-1 overflow-y-auto p-4 space-y-2 text-sm">
        <a href="../dashboard/index.ejs"
           class="flex items-center gap-3 rounded-lg px-4 py-3 transition-all <%= activeSection === 'dashboard' ? 'bg-terminal-yellow text-black font-bold' : 'text-gray-300 hover:bg-gray-800' %>">
            <span>📊</span>
            <span>Dashboard</span>
        </a>

        <a href="../operacion/empleados.ejs"
           class="flex items-center gap-3 rounded-lg px-4 py-3 transition-all <%= activeSection === 'empleados' ? 'bg-terminal-yellow text-black font-bold' : 'text-gray-300 hover:bg-gray-800' %>">
            <span>👷</span>
            <span>Personal operativo</span>
        </a>

        <a href="../operacion/zonas.ejs"
           class="flex items-center gap-3 rounded-lg px-4 py-3 transition-all <%= activeSection === 'zonas' ? 'bg-terminal-yellow text-black font-bold' : 'text-gray-300 hover:bg-gray-800' %>">
            <span>📍</span>
            <span>Zonas y Áreas</span>
        </a>

        <a href="../operacion/turnos.ejs"
           class="flex items-center gap-3 rounded-lg px-4 py-3 transition-all <%= activeSection === 'turnos' ? 'bg-terminal-yellow text-black font-bold' : 'text-gray-300 hover:bg-gray-800' %>">
            <span>⏱️</span>
            <span>Catálogo de Turnos</span>
        </a>

        <a href="../operacion/programacion.ejs"
           class="flex items-center gap-3 rounded-lg px-4 py-3 transition-all <%= activeSection === 'programacion' ? 'bg-terminal-yellow text-black font-bold' : 'text-gray-300 hover:bg-gray-800' %>">
            <span>🗓️</span>
            <span>Planilla Programación</span>
        </a>

        <a href="../admin/settings.ejs"
           class="flex items-center gap-3 rounded-lg px-4 py-3 transition-all <%= activeSection === 'configuracion' ? 'bg-terminal-yellow text-black font-bold' : 'text-gray-300 hover:bg-gray-800' %>">
            <span>⚙️</span>
            <span>Configuración</span>
        </a>
    </nav>

    <div class="p-4 border-t border-gray-800">
        <button id="btnLogout"
                class="w-full flex items-center justify-center gap-2 px-4 py-3 rounded-lg bg-red-600/90 hover:bg-red-600 text-white font-semibold transition-all">
            <span>⎋</span>
            <span>Cerrar sesión</span>
        </button>
    </div>
</aside>